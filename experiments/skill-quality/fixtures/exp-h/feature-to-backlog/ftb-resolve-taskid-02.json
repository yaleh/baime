{
  "id": "ftb-resolve-taskid-02",
  "skill": "feature-to-backlog",
  "taskClass": "A",
  "taskType": "binary-gate",
  "decisionPoint": "resolveOrCreate",
  "specSection": "resolveOrCreate :: Topic → (Task, EntryPoint)\nresolveOrCreate(T) =\n  | isTaskId(T) → (lookupTask(T), fromStatus(lookupTask(T).status))\n  | otherwise   → (createTask(T), ProposalLoop)\n\n-- Answer vocabulary: which branch fires?\n--   \"isTaskId\"  — topic is an existing task ID (matches ^task-\\d+$ pattern)\n--   \"otherwise\" — topic is a new feature description",
  "input": {
    "topic": "add OAuth2 login support"
  },
  "ground_truth_rationale": "Topic is a free-form description, not matching ^task-\\d+$. resolveOrCreate takes the otherwise branch: createTask and ProposalLoop entry point. Clearly NOT a task ID.",
  "answer": "otherwise",
  "answerType": "exact",
  "fixtureClass": "CLEAR"
}
